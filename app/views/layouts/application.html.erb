<!DOCTYPE html>
<html>
<head>
  <title>BookShelves</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="<%= root_path %>">BookShelves</a>
      </div>
      <div>
        <ul class="nav navbar-nav">
          <li class="<%= 'active' if current_page?(new_shelf_path) %>"><%= link_to "New Shelf", new_shelf_path %></li>
          <% Shelf.all.each do |shelf| %> 
           <li class="<%= 'active' if current_page?(shelf) %>"><%= link_to shelf.name, shelf %></li>
          <% end %>

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">User Shelves <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <% Shelf.all.each do |shelf| %>
                <li><%= link_to shelf.name, shelf %></li>
              <% end %>  
              
            </ul>
          </li>

        </ul>
      </div>
    </div>
  </nav>
  

  <%= yield %>
</div>
</body>
</html>
